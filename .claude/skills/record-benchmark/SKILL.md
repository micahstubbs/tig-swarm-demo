---
name: record-benchmark
description: Alias for rb - Run benchmark and record timestamped results
---

# Record Benchmark (Alias)

This is an alias for the `rb` skill.

## Usage

Use `/record-benchmark` or `/rb` - both invoke the same skill.

## Instructions

When this skill is invoked, immediately use the Skill tool to run `rb` with the provided arguments.
